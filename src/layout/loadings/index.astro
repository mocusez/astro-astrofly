---
// import { theme } from '../../theme'; // 假设 theme 是一个导入的主题配置
import FullpageLoading from "./fullpage-loading.astro"
---

<FullpageLoading />
<!-- <include src="./fullpage-loading.astro" /> -->

<!-- {theme.preloader.enable ? (
  theme.preloader.source === 1 ? (
    <include src="./fullpage-loading.astro" />
  ) : (
    <include src="./pace.astro" />
  )
) : null} -->